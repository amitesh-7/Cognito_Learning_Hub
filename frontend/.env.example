# Vite Environment Variables for QuizWise-AI Frontend

# Backend API URL
VITE_API_URL=http://localhost:3001

# Socket.IO Server URL (same as backend)
VITE_SOCKET_URL=http://localhost:3001

# Google Client ID (for OAuth - if using)
# VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# Note: All environment variables for Vite must start with VITE_
# They will be exposed to the client-side code, so don't put sensitive secrets here
